name: Geocode new locations

on: issues

jobs:
  geocode:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Extract fields
        id: extract
        run: |
          echo "Test extract werkt"
